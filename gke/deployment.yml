apiVersion: apps/v1
kind: Deployment
metadata:
 name: sdt-deployment
 namespace: sdt
spec:
 replicas: 1
 selector:
   matchLabels:
     app: sdt
 template:
   metadata:
     labels:
       app: sdt
   spec:
     containers:
     - name: sdt
       image: gcr.io/sask-dev-talks/sdt:0be2bdd5-b7e0-4d21-9f92-9b7868b58400
       ports:
       - containerPort: 11000
       livenessProbe:
        httpGet:
          path: /healthz
          port: 11000
        initialDelaySeconds: 3
        periodSeconds: 3
       readinessProbe:
        httpGet:
          path: /healthz
          port: 11000
        initialDelaySeconds: 3
        periodSeconds: 3
       resources:
        limits:
          memory: "128Mi"
          cpu: "250m"
